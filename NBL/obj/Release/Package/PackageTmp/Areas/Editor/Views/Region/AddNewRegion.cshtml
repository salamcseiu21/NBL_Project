
@model NblClassLibrary.Models.Region
@{
    ViewBag.Title = "Add New Region";
    Layout = "~/Views/Shared/_UserLayoutPage.cshtml";
}

<section class="content">

    <h3 class="text-center text-green headingStyle">Add New Region</h3>
    <hr />
    @using (Html.BeginForm())
    {
        <div class="form-horizontal">
               <div class="col-md-6 col-md-offset-3">
                   <div class="form-group">
                       @Html.LabelFor(m=>m.DivisionId,new {@class="control-label col-md-3"})
                       <div class="col-md-9">
                           @Html.DropDownListFor(m=>m.DivisionId,null,"--Select--",new {@class="form-control"})
                           @Html.ValidationMessageFor(m=>m.DivisionId,"",new {@class="text-danger"})
                       </div>
                   </div>
                   <div class="form-group">
                       @Html.LabelFor(m=>m.RegionName,new {@class="control-label col-md-3"})
                       <div class="col-md-9">
                           @Html.TextBoxFor(m=>m.RegionName,new {@class="form-control",placeholder= "Enter region Name" })
                           @Html.ValidationMessageFor(m=>m.RegionName,"",new {@class="text-danger"})
                       </div>
                   </div>
                   <div class="form-group">
                       <label class="control-label col-md-2"></label>
                       <div class="col-md-10 text-right">
                           @Html.ActionLink("Back", "All", "Region", new { area = "Editor" }, new { @class = "btn btn-info" })
                           <input type="submit" value="Save" class="btn btn-primary" />
                       </div>
                   </div>

               </div>
        </div>
    }

</section>
@section Scripts{
    <script>
        $(function () {
            //Initialize Select2 Elements
            $('.select2').select2();
        })
    </script>
}